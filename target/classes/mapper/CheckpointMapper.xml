<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="yumi.mapper.CheckpointMapper">

    <select id="selectByThreadIdOrderBySeq" resultType="yumi.entity.CheckpointEntity">
        SELECT checkpoint_seq, checkpoint_id, thread_id, node_id, next_node_id, state_data, state_data_json, saved_at
        FROM GRAPH_CHECKPOINT
        WHERE thread_id = #{threadId}
        ORDER BY checkpoint_seq ASC
    </select>

    <select id="selectLatestByThreadId" resultType="yumi.entity.CheckpointEntity">
        SELECT checkpoint_seq, checkpoint_id, thread_id, node_id, next_node_id, state_data, state_data_json, saved_at
        FROM GRAPH_CHECKPOINT
        WHERE thread_id = #{threadId}
        ORDER BY checkpoint_seq DESC
        LIMIT 1
    </select>

</mapper>