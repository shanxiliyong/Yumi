<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="yumi.mapper.ToolMapper">

    <resultMap id="ToolResultMap" type="yumi.entity.ToolEntity">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="type" column="type" />
        <result property="config" column="config" />
        <result property="permission" column="permission" />
        <result property="description" column="description" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="updateUser" column="update_user" />
    </resultMap>

    <select id="selectByName" resultMap="ToolResultMap">
        SELECT * FROM tool WHERE name = #{name}
    </select>

    <select id="selectAll" resultMap="ToolResultMap">
        SELECT * FROM tool ORDER BY update_time DESC
    </select>

    <select id="selectPage" resultMap="ToolResultMap">
        SELECT * FROM tool
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR type LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY update_time DESC
    </select>

    <select id="selectByType" resultMap="ToolResultMap">
        SELECT * FROM tool WHERE type = #{type}
    </select>

    <select id="selectByIds" resultMap="ToolResultMap">
        SELECT * FROM tool WHERE id IN
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <insert id="insertTool" parameterType="yumi.entity.ToolEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tool (name, type, config, permission, description, create_time, update_time, update_user)
        VALUES (#{name}, #{type}, #{config}, #{permission}, #{description}, #{createTime}, #{updateTime}, #{updateUser})
    </insert>

    <update id="updateTool" parameterType="yumi.entity.ToolEntity">
        UPDATE tool
        SET name = #{name},
            type = #{type},
            config = #{config},
            permission = #{permission},
            description = #{description},
            update_time = #{updateTime},
            update_user = #{updateUser}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM tool WHERE id = #{id}
    </delete>

</mapper>