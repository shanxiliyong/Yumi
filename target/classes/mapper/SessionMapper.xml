<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="yumi.mapper.SessionMapper">

    <select id="selectByUserId" resultType="yumi.entity.SessionEntity">
        SELECT * FROM chat_sessions WHERE user_id = #{userId} ORDER BY create_time DESC LIMIT 20
    </select>

    <update id="updateLastMessage">
        UPDATE chat_sessions
        SET last_message = #{lastMessage},
            update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>